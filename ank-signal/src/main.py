def add_together(x: int, y: int) -> int:
    z: int = x + y
    return z

def minus_together(x: int, y: int) -> int:
    """
    Subtract x from y

    Args:
        x: first numba
        y: second numba

    Returns:
        Yet another number
    """
    z: int = x - y
    return z
